Phogcache is an open-source, publicly available, extension to both OpenLayers and Leaflet. It makes enabling locally stored tile caches and seeding local caches extremely easy. The project makes use of the WebSQL and the PhoneGap JavaScript api for local file system access. It is targeted to work on Android, iOS, and Blackberry webkit-based native browsers via native app compilation through PhoneGap
